{
  "name": "npm-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "config": {
    "port": 5000
  },
  "scripts": {
    "tsc": "tsc",
    "prestart": "echo The prestart script is running && npm run tsc",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "sass": "^1.42.1"
  
  }
}
